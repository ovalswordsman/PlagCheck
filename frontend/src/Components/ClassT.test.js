import React from "react";
import { BrowserRouter, Routes } from 'react-router-dom';
import ClassT from "./ClassT";

it("renders without crashing", () => {
    <BrowserRouter>   
    <Routes>
        const div = document.createElement("div");
        ReactDOM.render(<ClassT />, div);
    </Routes>
    </BrowserRouter>
});
