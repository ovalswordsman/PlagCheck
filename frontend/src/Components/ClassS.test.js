import React from "react";
import { BrowserRouter, Routes } from 'react-router-dom';
import ClassS from "./ClassS";

it("renders without crashing", () => {
    <BrowserRouter>   
    <Routes>
        const div = document.createElement("div");
        ReactDOM.render(<ClassS />, div);
    </Routes>
    </BrowserRouter>
});
